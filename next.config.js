// /** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig

// next.config.js
module.exports = {
  webpack: (config, { isServer }) => {
    // Custom webpack configuration
    // Add loaders, plugins, etc.

    return config;
  },
  // Other custom configurations
};
